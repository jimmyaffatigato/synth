/*export interface EnvelopeAction {
    type: "SYNTH";
    env: string;
    a: [number, number];
    d: [number, number];
    s: [number, number];
    r: [number, number];
}

export const setEnvelope(env: string, a: [number, number], d: [])
*/
